%% LOAD 
[zs,tlapse,zstack,tsync,s2p] = load_drgs(511); 
load(s2p); 

%%

sample_rcthresh= prctile(redcell(:,2),75); 
red_vect = redcell(:,2)>sample_rcthresh; 
in_vect = red_vect; 
ex_vect = ~in_vect; 

id_vect= zeros(length(red_vect),1); 
id_vect(ex_vect)=1; 
id_vect(in_vect)=2; 
%id_vect([5 10 15])=3; 
%iscell(:,1)=1; % why do I do this 

%%

